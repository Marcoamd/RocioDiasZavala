---
interface Props {
  description?: string;
  title?: string;
  date?: string;
  type?: string;
}

const { description, title, date, type } = Astro.props;
---

<!-- <div
  class="flex-shrink-0 relative overflow-hidden bg-gradient-to-r from-violet-700 via-violet-800 to-violet-900 gap-8 rounded-lg border-2 border-purple-500 hover:border-fuchsia-500 hover:scale-105 transition-transform duration-700"
>
  <div class="relative pt-10 px-10 flex items-center justify-center">
    <p class="text-purple-200">
      {description}
    </p>
  </div>
  <div class="relative text-white px-6 pb-6 mt-6">
    <span class="block opacity-75 -mb-1 text-sm">{subtitle}</span>
    <div class="flex justify-between">
      <span class="block font-semibold text-lg">{title}</span>
      <span
        class="bg-white rounded-full text-purple-500 text-xs font-bold px-3 py-2 leading-none flex items-center"
        >{date}</span
      >
    </div>
  </div>
</div> --><!-- component -->
<div
  class="bg-gradient-to-r from-violet-600 via-violet-700 to-violet-800 gap-8 rounded-lg border-2 border-purple-500 hover:-translate-y-2 transition-transform duration-700 p-5"
>
  <div class="flex justify-between items-center">
    <span class="font-light text-violet-400 hidden lg:block text-sm"
      >{date}</span
    >
    <span
      class="bg-white rounded-full text-violet-500 text-xs font-bold px-3 py-2 leading-none flex items-center"
      >{type}</span
    >
  </div>
  <div class="py-10">
    <h3 class="text-lg text-white">
      {title}
    </h3>
    <p class="text-violet-200 text-sm font-light">
      {description}
    </p>
  </div>
  <div class="lg:hidden pt-5">
    <span class="text-violet-300">{date}</span>
  </div>
</div>
